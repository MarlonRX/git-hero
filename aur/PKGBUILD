# Maintainer: Marlon Ramirez <mramirezce14@gmail.com>
pkgname=gith
pkgver=0.4.0
pkgrel=1
pkgdesc="Simple Git repository manager TUI: see all your repos at a glance and run the everyday actions"
arch=('x86_64' 'aarch64')
url="https://github.com/MarlonRX/git-hero"
license=('MIT')
depends=('git')
makedepends=('cargo' 'rust')
optdepends=()
provides=('gith')
conflicts=('gith' 'git-hero')
source=("$pkgname-$pkgver.tar.gz::https://github.com/MarlonRX/git-hero/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('461092fddba47a46c02d7f7f55bb6f5c14f43101201099ccba6de195c2346848')

prepare() {
    cd "$pkgname-$pkgver"
    export RUSTUP_TOOLCHAIN=stable
    cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
    cd "$pkgname-$pkgver"
    export RUSTUP_TOOLCHAIN=stable
    export CARGO_HOME="$srcdir/cargo-home"
    cargo build --release --frozen
}

check() {
    cd "$pkgname-$pkgver"
    export RUSTUP_TOOLCHAIN=stable
    export CARGO_HOME="$srcdir/cargo-home"
    cargo test --release --frozen || true
}

package() {
    cd "$pkgname-$pkgver"
    install -Dm755 "target/release/gith" "$pkgdir/usr/bin/gith"
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}